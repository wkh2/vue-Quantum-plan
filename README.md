xman
====
Vue脚手架

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm start

# 打测试环境包
npm run dev

# 打预发环境包
npm run pre

# 打线上包
npm run build
# 此项目采用了vue-echarts结合的图标有swiper,tab等组件
